version: "3.8"

services:
  cloudflare:
    image: "cloudflare/cloudflared:2026.9.1"
    restart: "unless-stopped"
    command:
      - "tunnel"
      - "--no-autoupdate"
      - "run"
      - "--token"
      - "${CLOUDFLARE_TOKEN}"

networks:
  default:
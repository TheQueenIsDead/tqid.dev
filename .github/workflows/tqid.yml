name: "TQID"

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/tqid.yml
      - docker/tqid.yml
    branches:
      - main

jobs:
  tqid:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v7
      - run: docker compose -f docker/tqid.yml up --wait
